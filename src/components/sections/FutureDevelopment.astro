---
import Section from './Section.astro';
import { loadTranslations, type Language } from '../../i18n/utils';

export interface Props {
  currentLanguage?: Language;
}

const { currentLanguage = 'ja' } = Astro.props;
const translations = await loadTranslations(currentLanguage);
---

<Section id="future-development">
<div class="container mx-auto px-4 py-16">
    <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
            {translations.futureDevelopment.title}
        </h2>
        <p class="text-lg text-gray-600 max-w-2xl mx-auto">
            {translations.futureDevelopment.subtitle}
        </p>
    </div>
    
    <div class="max-w-6xl mx-auto">
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
            <!-- Edge AI -->
            <div class="bg-pink-50 rounded-2xl shadow-lg p-6 text-center">
                <div class="text-4xl mb-4">🧠</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies.edgeAI.title}</h3>
                <p class="text-gray-600 text-sm" set:html={translations.futureDevelopment.technologies.edgeAI.description.replace('、', '、<br>')}>
                </p>
            </div>
            
            <!-- 5G統合 -->
            <div class="bg-blue-50 rounded-2xl shadow-lg p-6 text-center">
                <div class="text-4xl mb-4">📡</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies["5gIntegration"].title}</h3>
                <p class="text-gray-600 text-sm" set:html={translations.futureDevelopment.technologies["5gIntegration"].description.replace('、', '、<br>')}>
                </p>
            </div>
            
            <!-- Digital Twin -->
            <div class="rounded-2xl shadow-lg p-6 text-center" style="background-color: #f0fdf4 !important;">
                <div class="text-4xl mb-4">🔄</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies.digitalTwin.title}</h3>
                <p class="text-gray-600 text-sm" set:html={translations.futureDevelopment.technologies.digitalTwin.description.replace('による、', 'による、<br>')}>
                </p>
            </div>
            
            <!-- 量子コンピューティング -->
            <div class="bg-purple-50 rounded-2xl shadow-lg p-6 text-center">
                <div class="text-4xl mb-4">⚛️</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies.quantumComputing.title}</h3>
                <p class="text-gray-600 text-sm">
                    {translations.futureDevelopment.technologies.quantumComputing.description}
                </p>
            </div>
            
            <!-- Brain-Machine Interface -->
            <div class="bg-orange-50 rounded-2xl shadow-lg p-6 text-center">
                <div class="text-4xl mb-4">🧬</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies.brainMachineInterface.title}</h3>
                <p class="text-gray-600 text-sm">
                    {translations.futureDevelopment.technologies.brainMachineInterface.description}
                </p>
            </div>
            
            <!-- ブロックチェーン統合 -->
            <div class="rounded-2xl shadow-lg p-6 text-center" style="background-color: #f9fafb !important;">
                <div class="text-4xl mb-4">🔗</div>
                <h3 class="text-xl font-bold text-gray-900 mb-3">{translations.futureDevelopment.technologies.blockchainIntegration.title}</h3>
                <p class="text-gray-600 text-sm" set:html={translations.futureDevelopment.technologies.blockchainIntegration.description.replace('した', 'した<br>')}>
                </p>
            </div>
        </div>
        
        <!-- R&Dロードマップ -->
        <div class="mt-16 bg-gradient-to-r from-blue-50 to-purple-50 rounded-2xl p-8">
            <h3 class="text-2xl font-bold text-center mb-8 text-gray-900">
                {translations.futureDevelopment.roadmap.title}
            </h3>
            <div class="grid md:grid-cols-3 gap-8">
                <div class="text-center">
                    <div class="bg-blue-500 text-white w-16 h-16 rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">
                        {translations.futureDevelopment.roadmap.shortTerm.year}
                    </div>
                    <h4 class="font-semibold text-gray-900 mb-2">{translations.futureDevelopment.roadmap.shortTerm.title}</h4>
                    <ul class="text-sm text-gray-600 space-y-1">
                        {translations.futureDevelopment.roadmap.shortTerm.items.map((item: string) => (
                            <li>• {item}</li>
                        ))}
                    </ul>
                </div>
                
                <div class="text-center">
                    <div class="bg-orange-500 text-white w-16 h-16 rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">
                        {translations.futureDevelopment.roadmap.mediumTerm.year}
                    </div>
                    <h4 class="font-semibold text-gray-900 mb-2">{translations.futureDevelopment.roadmap.mediumTerm.title}</h4>
                    <ul class="text-sm text-gray-600 space-y-1">
                        {translations.futureDevelopment.roadmap.mediumTerm.items.map((item: string) => (
                            <li>• {item}</li>
                        ))}
                    </ul>
                </div>
                
                <div class="text-center">
                    <div class="bg-purple-500 text-white w-16 h-16 rounded-full flex items-center justify-center text-xl font-bold mx-auto mb-4">
                        {translations.futureDevelopment.roadmap.longTerm.year}
                    </div>
                    <h4 class="font-semibold text-gray-900 mb-2">{translations.futureDevelopment.roadmap.longTerm.title}</h4>
                    <ul class="text-sm text-gray-600 space-y-1">
                        {translations.futureDevelopment.roadmap.longTerm.items.map((item: string) => (
                            <li>• {item}</li>
                        ))}
                    </ul>
                </div>
            </div>
        </div>
        
        <!-- パートナーシップ -->
        <div class="mt-12 bg-yellow-50 rounded-2xl shadow-lg p-8">
            <h3 class="text-2xl font-bold text-center mb-8 text-gray-900">
                {translations.futureDevelopment.partnership.title}
            </h3>
            <div class="grid md:grid-cols-2 gap-8">
                <div class="text-center p-6 bg-white rounded-lg">
                    <h4 class="text-lg font-semibold text-gray-900 mb-3">{translations.futureDevelopment.partnership.universities.title}</h4>
                    <p class="text-sm text-gray-600">
                        {translations.futureDevelopment.partnership.universities.description}
                    </p>
                </div>
                <div class="text-center p-6 bg-white rounded-lg">
                    <h4 class="text-lg font-semibold text-gray-900 mb-3">{translations.futureDevelopment.partnership.companies.title}</h4>
                    <p class="text-sm text-gray-600">
                        {translations.futureDevelopment.partnership.companies.description}
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
</Section>
