---
interface Props {
  isUnderline?: boolean;
}

const { isUnderline = false } = Astro.props as Props;
---

<div class={`rounded-[45px] ${isUnderline ? 'border border-dark shadow-[0px_5px_0px_#191a23]' : ''}`}>
  <slot />
</div>
